{
  "name": "v-cel",
  "version": "0.1.0",
  "description": "Remote chat mobile para sessões Antigravity via CDP",
  "type": "module",
  "scripts": {
    "start:web": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "chrome-remote-interface": "^0.33.3",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
